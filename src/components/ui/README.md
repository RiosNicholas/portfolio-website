# `components/ui`

This folder is reserved for shadcn/ui primitives.

Suggested next step:

1. Run `npx shadcn@latest init`
2. Add components such as `button`, `card`, and `badge`
3. Replace current scaffold blocks with shadcn primitives
